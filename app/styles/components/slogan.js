import { StyleSheet } from 'react-native'

const slogan = StyleSheet.create({
  text: {
    fontSize: 28,
    alignSelf: 'center',
    color: 'white',
    fontFamily: 'europa-regular',
    paddingTop: 10
  }
})

export default slogan